// An example included blueprint

// Any melee weapon in the game
item MeleeWeapon {
  attribute Damage([10-20]);
}

// Any ranged weapon in the game
item RangedWeapon {
  attribute Damage([10-20]);
  attribute Range([2-4]);
  attribute TargetMask(#tag Player);
  attribute ObstacleMask(Layer.Tiles, Layer.Traps);
}
