// An example included blueprint

// Any melee weapon in the game
item MeleeWeapon {
  attribute Damage(10);
}

// Any ranged weapon in the game
item RangedWeapon {
  attribute Damage(10);
  attribute Range(2);
  attribute TargetMask(Player);
  attribute ObstacleMask(Tiles, Traps);
}
