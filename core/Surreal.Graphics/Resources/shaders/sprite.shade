// Standard purpose sprite shader.

#shader_type sprite;

uniform vec4 _Tint;

void vertex(vec3 position) {
  POSITION = position;
}

void fragment(vec2 uv) {
  COLOR = SAMPLE(TEXTURE0, uv) * _Tint;
}
