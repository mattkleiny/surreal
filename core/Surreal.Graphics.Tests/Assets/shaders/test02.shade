// A more advanced sprite shader, for testing purposes.

#include "resx://Surreal.Graphics/Resources/shaders/common.shade";

#shader_type sprite;

uniform float flash_amount;
uniform vec4  flash_color1;
uniform vec4  flash_color2;
uniform float flash_cutoff;

void fragment() {
//	vec4 color  = SAMPLE(TEXTURE, UV);
//	vec3 output = color.rgb; // we're not touching the alpha channel
//
//	// flash the sprite's luminous parts based on the given flash color
//	if (flash_amount > 0.0f) {
//		float luminance = luminance(output);
//
//		if (luminance > flash_cutoff) {
//			output = mix(color.rgb, flash_color1.rgb, flash_amount);
//		} else {
//			output = mix(color.rgb, flash_color2.rgb, flash_amount);
//		}
//	}
//
//	COLOR = vec4(output, color.a);
}
